[project]
name = "minetrack"
version = "0.2.0"
description = "MineTrack"
requires-python = ">=3.9"
dependencies = [
    "flask>=3.0.0",
    "flask-cors>=4.0.0",
    "waitress>=3.0.0",
    "apscheduler>=3.10.0",
]

[project.scripts]
minetrack = "backend.main:main"